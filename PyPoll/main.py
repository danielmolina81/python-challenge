



Dim Total_Rows As Double
Dim Total_Votes As Double
Dim Candidates As Scr
Set Candidates = CreateObject("Unique Candidates")

Total_Rows = Cells(Rows.Count, 1).End(xlUp).Row
Total Votes = Total_Rows-1


for i = 2 to Total_Rows
    If Cells(i+1,3).value<> Cells(i,3).value
        Candidates.add "Cells (i,3).Value"

    Total_Change = Total_Change + Change

Next i
